<div class="content">
    <section class="js">
        <h2>JavaScript</h2>
        <div class="articles">
            {{!-- {{if articles}}
            {{#each articles}}
            {{>article}}
            {{else}}
            <h3 class="no-articles">No articles yet</h3>
            {{/if}} --}}
            <h3 class="no-articles">No articles yet</h3>
        </div>
    </section>
    <section class="CSharp">
        <h2>C#</h2>
        <div class="articles">
            {{!-- {{if articles}}
            {{#each articles}}
            {{>article}}
            {{else}}
            <h3 class="no-articles">No articles yet</h3>
            {{/if}} --}}
        </div>
    </section>
    <section class="Java">
        <h2>Java</h2>
        <div class="articles">
            {{!-- {{if articles}}
            {{#each articles}}
            {{>article}}
            {{else}}
            <h3 class="no-articles">No articles yet</h3>
            {{/if}} --}}
        </div>
    </section>
    <section class="Pyton">
        <h2>Pyton</h2>
        <div class="articles">
            {{!-- {{if articles}}
            {{#each articles}}
            {{>article}}
            {{else}}
            <h3 class="no-articles">No articles yet</h3>
            {{/if}} --}}
        </div>
    </section>
</div>